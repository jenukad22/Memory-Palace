# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# store submission credentials (Play Console service-account key, local EAS
# credentials manifest) — never commit these, reference by path only
credentials.json
*-service-account.json
google-play-*.json

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# env
.env*.local

# typescript
*.tsbuildinfo

# test coverage
coverage/

# local sqlite artifacts (expo-sqlite runtime files)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# OS
.DS_Store
Thumbs.db

# local env (Supabase sync config) — .env.example is the committed template
.env
.env.local
