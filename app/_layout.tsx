import { Stack } from 'expo-router';

// Root navigator. Placeholder configuration only — styling and screen options
// are blocked on DESIGN.md.
export default function RootLayout() {
  return <Stack />;
}
