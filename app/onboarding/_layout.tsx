import { Stack } from 'expo-router';

// Baseline assessment flow (SPEC.md sec 1): VVIQ -> digit span -> Corsi, one
// session with finish-later checkpoints between instruments.
export default function OnboardingLayout() {
  return <Stack />;
}
